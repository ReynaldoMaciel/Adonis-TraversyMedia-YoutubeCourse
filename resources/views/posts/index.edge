@layout('main')

@section('content')

<h2>{{ title }}</h2>
<br>
@if(old('notification'))
    <div class="alert alert-success">
        {{ old('notification')}}
    </div> 
@endif

<ul class="list-group">
@each(post in posts)
    <li  class="list-group-item">
        <a href="{{ route('PostController.details', {id : post.id}) }}">{{post.title}}</a>
    </li>
@endeach
</ul>

@endsection